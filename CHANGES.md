# Andalusi — الهوية البصرية الجديدة (Mint & Forest Green) + التجاوب الكامل

## 1) باليتة الألوان الجديدة (Light Mode)
- **الخلفيات**: خلفية التطبيق (`TravelBackground.tsx`) وكل كاردات التطبيق
  استبدلت من تدرجات كهرمانية/برتقالية باهتة إلى تدرجات نعناعية ناعمة:
  `from-emerald-50 via-teal-50/70 to-green-50` (خلفية عامة) و
  `from-white via-emerald-50/60 to-teal-50/40` (كاردات) — مطبّقة عبر
  13 ملفاً بشكل موحّد.
- **العناصر النشطة والأزرار الرئيسية**: أخضر غابي عميق
  (`from-emerald-700 to-green-800`) في: العنصر النشط بالـ Sidebar،
  زر "Switch Account"، تبويبات Starred (Words/Idioms)، تبويبات AuthPage
  (Sign In/Sign Up)، وكل أزرار الـ CTA الرئيسية (Dashboard، LessonPage،
  LessonQuiz، Quiz، PlacementTest، AuthPage).
- الألوان الوظيفية (البنفسجي/الأزرق السماوي لصوت الإنجليزي، الوردي
  لصوت الإسباني والإجابات الخاطئة، الكهرماني لأيقونة "Star" وتنبيهات
  "لم يتم تحديد مستوى بعد") أُبقيت كما هي لأنها ترميز وظيفي ثابت في
  التطبيق، وليست جزءاً من هوية "الزر الرئيسي/العنصر النشط".

## 2) توحيد الكاردات (من الجلسة السابقة، مؤكَّد التوافق مع الثيم الجديد)
Grammar / Idioms / Starred / Flashcards / Quiz / Learn — كلها بنفس شكل
الكارد (حدود مدورة، تدرج نعناعي، hover برفع خفيف وظل) بعد تطبيق الباليتة
الجديدة.

## 3) الـ Popup / Modal (مؤكَّد من الجلسة السابقة)
`Modal.tsx` المشترك ما زال يُستخدم فور الضغط على أي كارد في Grammar،
Idioms، Starred، فتح الدرس، واختبار الدرس — بنفس سلوك الإغلاق (X، النقر
خارج الكارد، Escape).

## 4) التوافق التام مع الشاشات (جديد في هذه الجلسة)
- **Sidebar متجاوب بالكامل**: على الشاشات الصغيرة (أصغر من `lg`)
  تختفي القائمة الجانبية وتتحول لشريط علوي بزر Hamburger؛ الضغط عليه
  يفتح قائمة منسدلة (Drawer) من اليسار فوق طبقة معتّمة (Overlay)،
  وتُغلق بالنقر على X، النقر على الـ Overlay، أو اختيار أي صفحة.
  على الشاشات الكبيرة (`lg` فأعلى) تعود القائمة لشكلها الثابت الأصلي.
- **`App.tsx`**: التخطيط العام تحول من صف أفقي دائم إلى عمود على
  الموبايل وصف أفقي على الشاشات الكبيرة (`flex-col lg:flex-row`)
  حتى يتماشى مع الشريط العلوي الجديد بدل التصادم معه.
- **الشبكات (Grid Cards)**: تم التأكد من أن كل صفحة تستخدم
  `grid-cols-1` كنقطة بداية على الموبايل (Grammar، Idioms، Starred،
  Learn) وتتوسع تدريجياً (`sm:` / `md:` / `lg:`)، وتم تعديل بطاقات
  "Quick Stats" في Dashboard لتكون `grid-cols-1 sm:grid-cols-3` بدل
  ثلاثة أعمدة ثابتة دائماً.

## 5) الاستقرار البرمجي
بيئة العمل بلا اتصال إنترنت في هذه الجلسة أيضاً، فلم يكن ممكناً تشغيل
`npm install` / `npm run build` فعلياً. بدلاً من ذلك:
- فحص برمجي لتوازن الأقواس `{} () []` على **كل** الملفات المعدَّلة
  (17 ملفاً) — **متوازنة بالكامل**.
- فحص برمجي لتوازن وسوم JSX (`div`, `button`, `aside`, `header`, `nav`,
  إلخ) خصوصاً في `Sidebar.tsx` المُعاد بناؤه بالكامل — **متوازنة
  بالكامل** (الفارق الوحيد فيha `div` واحد self-closing للـ Overlay،
  وهو سلوك متوقع وصحيح).
- مراجعة يدوية سطراً بسطر لكل التعديلات اللونية للتأكد من عدم كسر أي
  className أو بنية JSX.

**قبل النشر، الرجاء تشغيل `npm install && npm run build` على جهازك
للتأكد النهائي 100%.**
